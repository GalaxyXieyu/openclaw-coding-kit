---
phase: 02-cross-platform-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/pm/scripts/pm_runtime.py
  - skills/pm/scripts/pm.py
  - skills/pm/scripts/pm_gsd.py
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
user_setup: []
must_haves:
  truths:
    - OpenClaw/Codex/GSD 关键二进制或脚本路径不再优先依赖作者本机目录
    - 运行时路径发现顺序对 Windows/macOS/Linux 都有清晰 fallback
  artifacts:
    - skills/pm/scripts/pm_runtime.py
    - skills/pm/scripts/pm.py
    - skills/pm/scripts/pm_gsd.py
  key_links:
    - PM runtime、bridge script 和 GSD tools 的路径发现策略保持一致的优先级模型
---

<objective>
收敛运行时二进制与桥接脚本的路径发现逻辑，建立跨平台可解释的基础策略。

Purpose: 先解决 CLI/script 入口的本机耦合问题，为后续状态目录和文档对齐打基础。
Output: 更合理的运行时路径发现实现与验证命令。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cross-platform-runtime/02-CONTEXT.md
@.planning/codebase/CONCERNS.md
@skills/pm/scripts/pm_runtime.py
@skills/pm/scripts/pm.py
@skills/pm/scripts/pm_gsd.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize CLI/script path discovery priorities</name>
  <files>skills/pm/scripts/pm_runtime.py, skills/pm/scripts/pm.py, skills/pm/scripts/pm_gsd.py</files>
  <read_first>.planning/codebase/CONCERNS.md, .planning/phases/02-cross-platform-runtime/02-CONTEXT.md</read_first>
  <action>把 OpenClaw CLI、Codex CLI、GSD tools、bridge script 的候选路径策略统一为“环境变量或显式配置优先，其次 PATH/system discovery，再次平台候选兜底”。避免继续把 `.nvm` 版本目录、`/opt/homebrew`、`~/.codex/...` 当作唯一主路径。必要时引入一个集中 helper，但要保持最小侵入。</action>
  <verify>rg -n \"OPENCLAW|CODEX|GSD|which|where|shutil\\.which|env\" skills/pm/scripts/pm_runtime.py skills/pm/scripts/pm.py skills/pm/scripts/pm_gsd.py</verify>
  <acceptance_criteria>
    - 二进制发现逻辑不再只依赖作者本机固定路径
    - 至少支持环境变量覆盖与 PATH 发现
    - Windows 兼容被纳入 fallback 设计，而不是完全缺席
  </acceptance_criteria>
  <done>运行时入口的路径发现模型已统一</done>
</task>

<task type="auto">
  <name>Task 2: Verify runtime discovery remains readable and debuggable</name>
  <files>skills/pm/scripts/pm_runtime.py, skills/pm/scripts/pm.py, skills/pm/scripts/pm_gsd.py</files>
  <read_first>skills/pm/scripts/pm_runtime.py</read_first>
  <action>在不污染主流程的前提下，让错误信息和 fallback 行为更可解释。重点是让用户知道缺了什么、下一步该设哪个环境变量或检查哪个命令，而不是只看到“找不到二进制”。</action>
  <verify>python3 skills/pm/scripts/pm.py --help && python3 skills/pm/scripts/pm.py route-gsd --repo-root .</verify>
  <acceptance_criteria>
    - 入口脚本仍可启动
    - 路径发现失败时的错误信息可指导用户自助修复
  </acceptance_criteria>
  <done>运行时路径治理既可用也可排查</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 skills/pm/scripts/pm.py --help`
- [ ] `python3 skills/pm/scripts/pm.py route-gsd --repo-root .`
- [ ] 关键运行时路径发现逻辑已统一为跨平台友好模型
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Runtime path discovery is no longer authored around a single-machine assumption
</success_criteria>

<output>
After completion, create `.planning/phases/02-cross-platform-runtime/02-01-SUMMARY.md`
</output>
