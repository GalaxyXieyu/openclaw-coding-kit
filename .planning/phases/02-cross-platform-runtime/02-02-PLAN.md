---
phase: 02-cross-platform-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - skills/pm/scripts/pm_auth.py
  - skills/pm/scripts/pm_config.py
  - skills/pm/scripts/pm_io.py
  - skills/coder/scripts/observe_acp_session.py
  - skills/pm/scripts/pm_workspace.py
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
user_setup: []
must_haves:
  truths:
    - 配置文件、状态目录、OpenClaw state 读取路径对跨平台布局有明确兼容策略
    - repo-local 与 user-global 路径边界更加清晰
  artifacts:
    - skills/pm/scripts/pm_auth.py
    - skills/pm/scripts/pm_config.py
    - skills/pm/scripts/pm_io.py
    - skills/coder/scripts/observe_acp_session.py
    - skills/pm/scripts/pm_workspace.py
  key_links:
    - 配置发现、状态目录发现、workspace 默认路径遵循一致的平台策略
---

<objective>
处理配置、状态目录和 workspace 默认路径中的跨平台差异，减少用户主目录和单平台布局耦合。

Purpose: 让真正影响日常使用的 state/config 路径不再默认只适配作者当前环境。
Output: 更清晰的 config/state/workspace 发现逻辑。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cross-platform-runtime/02-CONTEXT.md
@.planning/phases/02-cross-platform-runtime/02-01-SUMMARY.md
@.planning/codebase/CONCERNS.md
@skills/pm/scripts/pm_auth.py
@skills/pm/scripts/pm_config.py
@skills/pm/scripts/pm_io.py
@skills/coder/scripts/observe_acp_session.py
@skills/pm/scripts/pm_workspace.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize config and state directory discovery</name>
  <files>skills/pm/scripts/pm_auth.py, skills/pm/scripts/pm_config.py, skills/pm/scripts/pm_io.py, skills/coder/scripts/observe_acp_session.py</files>
  <read_first>.planning/codebase/CONCERNS.md, skills/pm/scripts/pm_config.py</read_first>
  <action>为 `openclaw.json`、PM state、OpenClaw state 等路径增加更合理的跨平台候选与环境变量覆盖，确保 Windows/macOS/Linux 都能理解默认行为。要显式区分 repo-local 与 user-global 路径，并避免继续只围绕 `~/.openclaw` / `~/.codex` 这种单一前提设计报错文案。</action>
  <verify>python3 skills/pm/scripts/pm.py context --help && python3 skills/coder/scripts/observe_acp_session.py --help</verify>
  <acceptance_criteria>
    - config/state 路径发现支持更明确的 override 和 fallback
    - Windows 用户不会被完全 Unix 化的报错文案误导
    - 入口脚本帮助命令仍可正常工作
  </acceptance_criteria>
  <done>配置与状态目录发现具备跨平台基本兼容性</done>
</task>

<task type="auto">
  <name>Task 2: Rework workspace default path assumptions</name>
  <files>skills/pm/scripts/pm_workspace.py</files>
  <read_first>skills/pm/scripts/pm_workspace.py, .planning/codebase/CONCERNS.md</read_first>
  <action>检查 workspace 默认路径和模板根路径推导，优先解决会阻碍跨平台接入或误导用户的路径假设。模板资产本身如果暂时无法完全内置，也要让路径来源更显式、错误更可解释、默认 workspace 对 Windows 用户更合理。</action>
  <verify>python3 skills/pm/scripts/pm.py init --project-name demo --dry-run</verify>
  <acceptance_criteria>
    - workspace 默认路径不再只体现当前作者环境
    - 相关错误或 dry-run 输出能帮助用户理解路径来源
  </acceptance_criteria>
  <done>workspace 路径假设被收敛到可解释范围内</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 skills/pm/scripts/pm.py context --help`
- [ ] `python3 skills/coder/scripts/observe_acp_session.py --help`
- [ ] `python3 skills/pm/scripts/pm.py init --project-name demo --dry-run`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- State/config/workspace discovery no longer assumes one host layout
</success_criteria>

<output>
After completion, create `.planning/phases/02-cross-platform-runtime/02-02-SUMMARY.md`
</output>
