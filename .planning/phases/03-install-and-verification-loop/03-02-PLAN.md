---
phase: 03-install-and-verification-loop
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - INSTALL.md
  - README.md
  - skills/pm/SKILL.md
  - skills/pm/scripts/pm_cli.py
  - skills/pm/scripts/pm_commands.py
  - skills/pm/scripts/pm_workspace.py
autonomous: true
requirements:
  - BOOT-03
  - BOOT-04
user_setup: []
must_haves:
  truths:
    - 安装后用户知道先跑哪些无鉴权命令进行 bootstrap 和诊断
    - 中文项目名、workspace 模板缺失、repo-local 与 user-global 边界都有可操作说明
    - 如有必要，CLI 帮助或 dry-run 输出会把诊断信息暴露得更清楚
  artifacts:
    - INSTALL.md
    - README.md
    - skills/pm/SKILL.md
    - skills/pm/scripts/pm_cli.py
    - skills/pm/scripts/pm_commands.py
    - skills/pm/scripts/pm_workspace.py
  key_links:
    - `pm init --dry-run` 是安装后的主 bootstrap 入口
    - 本地无鉴权验证先于真实 Feishu/OpenClaw 绑定
---

<objective>
补全安装后初始化与诊断步骤，让用户能在不依赖真实线上资源的前提下确认仓库是否已正确落地。

Purpose: 让安装文档从“能看懂概念”升级为“能照着一步步排查和继续推进”的 operator 流程。
Output: 明确的 post-install bootstrap / diagnostics 顺序，以及必要时更可解释的 CLI 帮助或 dry-run 输出。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-install-and-verification-loop/03-CONTEXT.md
@.planning/phases/03-install-and-verification-loop/03-01-PLAN.md
@.planning/codebase/CONCERNS.md
@README.md
@INSTALL.md
@skills/pm/SKILL.md
@skills/pm/scripts/pm_cli.py
@skills/pm/scripts/pm_commands.py
@skills/pm/scripts/pm_workspace.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the canonical local bootstrap and diagnostics sequence</name>
  <files>INSTALL.md, README.md, skills/pm/SKILL.md</files>
  <read_first>.planning/phases/03-install-and-verification-loop/03-CONTEXT.md, README.md, INSTALL.md</read_first>
  <action>把安装后的最小闭环整理成明确顺序：依赖检查、`pm init --project-name ... --dry-run`、`pm context --help` / `route-gsd` 等无鉴权诊断，再到真实 OpenClaw/Feishu 绑定。要显式区分“必须完成”和“可选增强”，并解释中文项目名何时需要 `--english-name`。文档里不能要求用户先拥有 Feishu 才能确认本地安装是否成功。</action>
  <verify>python3 skills/pm/scripts/pm.py init --project-name demo --dry-run && python3 skills/pm/scripts/pm.py init --project-name "测试项目" --english-name demo --dry-run</verify>
  <acceptance_criteria>
    - 用户能先完成本地 bootstrap 验证，再决定是否接入真实平台
    - 中文项目名与 ASCII slug 的约束被说清楚
    - 文档能解释 dry-run 中的 `template_root` / `template_root_exists`
  </acceptance_criteria>
  <done>安装后的主 bootstrap / diagnostics 流程清晰可执行</done>
</task>

<task type="auto">
  <name>Task 2: Tighten CLI diagnostics where current output is still ambiguous</name>
  <files>skills/pm/scripts/pm_cli.py, skills/pm/scripts/pm_commands.py, skills/pm/scripts/pm_workspace.py, INSTALL.md</files>
  <read_first>skills/pm/scripts/pm_cli.py, skills/pm/scripts/pm_commands.py, skills/pm/scripts/pm_workspace.py</read_first>
  <action>如果现有 `init --help`、`init --dry-run` 或 workspace bootstrap 相关输出仍不足以指导用户排查模板目录、workspace 根目录、英文名 slug 或 OpenClaw 配置来源，则做最小侵入的 CLI/文案增强。优先加强说明和错误信息，不引入新的复杂流程。</action>
  <verify>python3 skills/pm/scripts/pm.py init --help && python3 skills/pm/scripts/pm.py context --help && python3 skills/pm/scripts/pm.py context --refresh && python3 skills/pm/scripts/pm.py route-gsd --repo-root . --phase 3</verify>
  <acceptance_criteria>
    - 关键 CLI 帮助输出能指导用户自助排查安装问题
    - workspace 模板缺失被当成显式限制处理，而不是隐式失败
    - 修改不破坏现有命令面
  </acceptance_criteria>
  <done>安装诊断链路更可解释</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 skills/pm/scripts/pm.py init --help`
- [ ] `python3 skills/pm/scripts/pm.py init --project-name demo --dry-run`
- [ ] `python3 skills/pm/scripts/pm.py init --project-name "测试项目" --english-name demo --dry-run`
- [ ] `python3 skills/pm/scripts/pm.py context --refresh`
- [ ] `python3 skills/pm/scripts/pm.py route-gsd --repo-root . --phase 3`
- [ ] 安装文档中的 bootstrap / diagnostics 步骤可以按顺序执行
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Post-install diagnostics are clear enough that users can tell "what is missing" before touching real integrations
</success_criteria>

<output>
After completion, create `.planning/phases/03-install-and-verification-loop/03-02-SUMMARY.md`
</output>
