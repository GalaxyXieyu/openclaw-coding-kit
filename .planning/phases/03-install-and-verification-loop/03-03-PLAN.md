---
phase: 03-install-and-verification-loop
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - INSTALL.md
  - README.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/CONCERNS.md
  - skills/coder/scripts/observe_acp_session.py
autonomous: true
requirements:
  - QUAL-01
  - BOOT-03
user_setup: []
must_haves:
  truths:
    - 仓库至少有一组不依赖真实线上资源的最小 smoke checks
    - 本地无鉴权验证与真实 OpenClaw/Feishu 集成验证被明确拆开
    - 验证说明能作为分享和后续 Phase 5 的质量基线
  artifacts:
    - INSTALL.md
    - README.md
    - .planning/codebase/TESTING.md
    - .planning/codebase/CONCERNS.md
  key_links:
    - smoke checks 与当前 CLI / dry-run / observer 入口一致
    - 测试文档与安装文档不再各说各话
---

<objective>
建立最小 smoke checks 和验证说明，为安装闭环提供低成本、可复制的质量基线。

Purpose: 当前仓库没有完整自动化测试框架，因此必须至少提供一组可信的本地检查和手动验证说明，避免“装上了但不知道算不算成功”。
Output: 一组清晰的 smoke checks、分层验证说明，以及同步更新后的 testing/concerns 文档。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-install-and-verification-loop/03-CONTEXT.md
@.planning/phases/03-install-and-verification-loop/03-01-PLAN.md
@.planning/phases/03-install-and-verification-loop/03-02-PLAN.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@INSTALL.md
@README.md
@skills/coder/scripts/observe_acp_session.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define and document the minimum no-auth smoke checks</name>
  <files>INSTALL.md, README.md, .planning/codebase/TESTING.md</files>
  <read_first>.planning/codebase/TESTING.md, INSTALL.md</read_first>
  <action>把当前已经可行的本地验证命令整理成一个最小 smoke check 集合，并明确命令顺序、预期结果和失败时的诊断方向。优先使用已存在命令：Python 语法检查、CLI help、`route-gsd`、`pm init --dry-run`、observer help。若确有必要，可对 observer/帮助文案做最小增强，但不要引入大型测试框架。</action>
  <verify>python3 -m py_compile skills/pm/scripts/*.py skills/coder/scripts/*.py && python3 skills/pm/scripts/pm.py --help && python3 skills/pm/scripts/pm.py context --help && python3 skills/pm/scripts/pm.py route-gsd --repo-root . --phase 3 && python3 skills/pm/scripts/pm.py init --project-name demo --dry-run && python3 skills/coder/scripts/observe_acp_session.py --help</verify>
  <acceptance_criteria>
    - 至少一组无鉴权 smoke checks 被集中记录
    - 每条命令都有“验证什么”的说明
    - 这些命令在当前仓库可直接运行
  </acceptance_criteria>
  <done>最小本地 smoke checks 已建立</done>
</task>

<task type="auto">
  <name>Task 2: Split verification guidance into local validation vs real integration validation</name>
  <files>INSTALL.md, .planning/codebase/TESTING.md, .planning/codebase/CONCERNS.md</files>
  <read_first>.planning/phases/03-install-and-verification-loop/03-CONTEXT.md, .planning/codebase/CONCERNS.md</read_first>
  <action>把验证说明分成两层：一层是本地无鉴权验证，确认仓库和 CLI 本身可用；另一层是依赖真实 OpenClaw/Feishu 的集成验证，确认 bridge / 会话回推 / 共享工作区链路是否接通。对尚未自动化覆盖的部分保留显式限制和后续建议，避免把“未覆盖”误写成“已验证”。</action>
  <verify>rg -n "smoke|验证|Feishu|OpenClaw|无鉴权|集成" INSTALL.md .planning/codebase/TESTING.md .planning/codebase/CONCERNS.md</verify>
  <acceptance_criteria>
    - 用户能区分本地安装成功与真实集成成功的边界
    - testing / concerns 文档同步反映当前真实覆盖范围
    - Phase 5 后续质量工作有可继承的基线
  </acceptance_criteria>
  <done>验证说明形成清晰分层</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m py_compile skills/pm/scripts/*.py skills/coder/scripts/*.py`
- [ ] `python3 skills/pm/scripts/pm.py --help`
- [ ] `python3 skills/pm/scripts/pm.py context --help`
- [ ] `python3 skills/pm/scripts/pm.py route-gsd --repo-root . --phase 3`
- [ ] `python3 skills/pm/scripts/pm.py init --project-name demo --dry-run`
- [ ] `python3 skills/coder/scripts/observe_acp_session.py --help`
- [ ] 本地无鉴权验证与真实集成验证已在文档中分层说明
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a pragmatic install verification baseline even without a full automated test harness
</success_criteria>

<output>
After completion, create `.planning/phases/03-install-and-verification-loop/03-03-SUMMARY.md`
</output>
