---
phase: 04-bridge-reporting-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/acp-progress-bridge/openclaw.plugin.json
  - examples/openclaw.json5.snippets.md
  - INSTALL.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements:
  - BRDG-01
  - BRDG-04
user_setup: []
must_haves:
  truths:
    - bridge 配置项的作用域、节流和完成策略有清晰契约
    - 当前默认作用域被明确表达为 Codex ACP 到 Feishu/main 父会话，而不是泛化成“全部 provider 默认支持”
    - 安装和示例文档不会再把 bridge 配置片段解释得比当前实现更宽
  artifacts:
    - plugins/acp-progress-bridge/openclaw.plugin.json
    - examples/openclaw.json5.snippets.md
    - INSTALL.md
    - .planning/codebase/INTEGRATIONS.md
    - .planning/codebase/ARCHITECTURE.md
  key_links:
    - schema 默认值、示例配置和架构说明必须一致
    - bridge 契约说明要能直接拿去分享，而不是只能靠读源码
---

<objective>
固化 bridge 运行链路与配置契约，让当前自动汇报机制的作用域、默认值和策略边界对外可解释。

Purpose: 先把“它现在到底怎么工作、这些配置到底控制什么”讲清楚，后续 observability 和 provider 扩展才不会建立在模糊前提上。
Output: 一组对外一致的 schema / examples / install / architecture / integration 说明。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bridge-reporting-hardening/04-CONTEXT.md
@plugins/acp-progress-bridge/openclaw.plugin.json
@plugins/acp-progress-bridge/index.ts
@examples/openclaw.json5.snippets.md
@INSTALL.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize bridge config contract across schema, examples, and docs</name>
  <files>plugins/acp-progress-bridge/openclaw.plugin.json, examples/openclaw.json5.snippets.md, INSTALL.md</files>
  <read_first>.planning/phases/04-bridge-reporting-hardening/04-CONTEXT.md, plugins/acp-progress-bridge/openclaw.plugin.json</read_first>
  <action>把 bridge 的关键配置面按“作用域、节流、完成策略”重新组织清楚。重点说明 `parentSessionPrefixes` / `childSessionPrefixes`、`deliverProgress` / `deliverCompletion`、`firstProgressDelayMs` / `progressDebounceMs` / `settleAfterDoneMs` / `replayCompletedWithinMs` 各自控制什么，以及当前默认值到底服务哪条主链路。必要时补 schema 描述或示例注释，但避免改动配置行为本身。</action>
  <verify>rg -n "parentSessionPrefixes|childSessionPrefixes|deliverProgress|deliverCompletion|settleAfterDoneMs|replayCompletedWithinMs" plugins/acp-progress-bridge/openclaw.plugin.json examples/openclaw.json5.snippets.md INSTALL.md</verify>
  <acceptance_criteria>
    - schema / examples / INSTALL 对关键配置的含义一致
    - 当前默认作用域不再被误读成“对所有 provider 开箱即用”
    - bridge 仍保持可选增强定位，而不是最小安装前置
  </acceptance_criteria>
  <done>bridge 配置契约清晰一致</done>
</task>

<task type="auto">
  <name>Task 2: Refresh architecture and integration docs for the current delivery chain</name>
  <files>.planning/codebase/INTEGRATIONS.md, .planning/codebase/ARCHITECTURE.md</files>
  <read_first>plugins/acp-progress-bridge/index.ts, .planning/codebase/INTEGRATIONS.md</read_first>
  <action>把当前自动汇报链路的“谁发现 run、谁构造内部消息、谁生成用户可见回复”写成清晰 delivery chain。必须明确 `[[acp_bridge_update]]` 是内部消息，父会话负责自然语言转译，避免分享和维护时继续把 plugin / PM / coder 的职责混淆。</action>
  <verify>rg -n "\\[\\[acp_bridge_update\\]\\]|parentSessionPrefixes|childSessionPrefixes|assistant_tail|spawnedBy" .planning/codebase/INTEGRATIONS.md .planning/codebase/ARCHITECTURE.md</verify>
  <acceptance_criteria>
    - 自动汇报链路可以按文档直接讲清楚
    - plugin 与父会话职责边界不再模糊
  </acceptance_criteria>
  <done>bridge 运行链路的文档化契约完成</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "parentSessionPrefixes|childSessionPrefixes|deliverProgress|deliverCompletion|settleAfterDoneMs|replayCompletedWithinMs" plugins/acp-progress-bridge/openclaw.plugin.json examples/openclaw.json5.snippets.md INSTALL.md`
- [ ] `rg -n "\\[\\[acp_bridge_update\\]\\]|assistant_tail|spawnedBy" .planning/codebase/INTEGRATIONS.md .planning/codebase/ARCHITECTURE.md`
- [ ] bridge 配置说明、示例和架构说明口径一致
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The bridge contract is understandable to operators and explainable in sharing material
</success_criteria>

<output>
After completion, create `.planning/phases/04-bridge-reporting-hardening/04-01-SUMMARY.md`
</output>
