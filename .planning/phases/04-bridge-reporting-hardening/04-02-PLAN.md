---
phase: 04-bridge-reporting-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - plugins/acp-progress-bridge/index.ts
  - INSTALL.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements:
  - BRDG-01
  - BRDG-02
  - BRDG-03
user_setup: []
must_haves:
  truths:
    - operator 能定位“为什么没有 progress / completion 回推”
    - bridge 的发现、节流、completion 处理和父会话关联至少有一条清晰的调试路径
    - 当前本地 smoke baseline 能延伸到 bridge 级调试，而不要求马上构建完整测试框架
  artifacts:
    - plugins/acp-progress-bridge/index.ts
    - INSTALL.md
    - .planning/codebase/TESTING.md
    - .planning/codebase/CONCERNS.md
  key_links:
    - 调试路径必须承接 Phase 3 已建立的本地验证思路
    - 代码日志/状态与 operator 文档说明必须相互印证
---

<objective>
强化 progress / completion 回推的可观测性与调试方式，让维护者能定位自动汇报失效的真实原因。

Purpose: 当前桥接逻辑能工作，但出了问题仍然过度依赖读源码。Phase 4 需要把“如何排查”做成稳定操作路径。
Output: 最小侵入的日志/状态增强与 operator 调试说明。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bridge-reporting-hardening/04-CONTEXT.md
@.planning/phases/04-bridge-reporting-hardening/04-01-PLAN.md
@plugins/acp-progress-bridge/index.ts
@INSTALL.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve bridge-side observability for discovery and delivery failures</name>
  <files>plugins/acp-progress-bridge/index.ts</files>
  <read_first>plugins/acp-progress-bridge/index.ts, .planning/phases/04-bridge-reporting-hardening/04-CONTEXT.md</read_first>
  <action>在不改变主行为模型的前提下，增强 bridge 对关键失败面的可观测性。重点覆盖：prefix 不匹配、缺 parent session id、progress 被节流、completion 尚未到 settle 窗口、replay 被跳过、宿主 CLI entrypoint 不可用等场景。优先做更清晰的日志、状态 summary 或最小辅助输出，不引入复杂监控系统。</action>
  <verify>rg -n "missing parent sessionId|progress delivered|completion delivered|OpenClaw CLI entrypoint|settleAfterDoneMs|replayCompletedWithinMs|progressDebounceMs" plugins/acp-progress-bridge/index.ts</verify>
  <acceptance_criteria>
    - 至少能从日志/状态中看出 run 是否被发现、progress 是否被发送、completion 是否被跳过
    - 常见失效原因有可读提示，而不是只能靠源码猜
  </acceptance_criteria>
  <done>bridge 可观测性增强完成</done>
</task>

<task type="auto">
  <name>Task 2: Document an operator debugging path for bridge failures</name>
  <files>INSTALL.md, .planning/codebase/TESTING.md, .planning/codebase/CONCERNS.md</files>
  <read_first>.planning/codebase/TESTING.md, .planning/codebase/CONCERNS.md, INSTALL.md</read_first>
  <action>补一条可执行的 operator 调试路径，明确从哪些文件和命令判断：run 是否存在、前缀是否匹配、session store / stream / transcript 是否更新、progress/completion 为什么没回推。要把“本地 bridge 级检查”和“真实 Feishu 可见结果”区分清楚。</action>
  <verify>rg -n "bridge|progress|completion|session|stream|transcript|调试" INSTALL.md .planning/codebase/TESTING.md .planning/codebase/CONCERNS.md</verify>
  <acceptance_criteria>
    - operator 不需要直接通读源码也能开始排查
    - 文档能区分 plugin 内部失效与外部 Feishu 可见结果缺失
  </acceptance_criteria>
  <done>bridge operator 调试路径可落地</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "missing parent sessionId|progress delivered|completion delivered|OpenClaw CLI entrypoint" plugins/acp-progress-bridge/index.ts`
- [ ] `rg -n "bridge|progress|completion|session|stream|transcript|调试" INSTALL.md .planning/codebase/TESTING.md .planning/codebase/CONCERNS.md`
- [ ] 当前 bridge 的常见失败路径有代码侧和文档侧双重解释
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Operators can diagnose bridge failures without reverse-engineering the entire plugin
</success_criteria>

<output>
After completion, create `.planning/phases/04-bridge-reporting-hardening/04-02-SUMMARY.md`
</output>
